{
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "php artisan optimize:clear && php artisan migrate --force && php artisan db:seed --force && /start-container.sh"
    }
}
