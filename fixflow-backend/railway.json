{
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "rm -f bootstrap/cache/routes-v7.php bootstrap/cache/config.php && php artisan route:cache && php artisan migrate --force && php artisan db:seed --force && /start-container.sh"
    }
}
