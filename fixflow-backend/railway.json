{
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "rm -f bootstrap/cache/*.php && php artisan migrate --force && php artisan db:seed --force && /start-container.sh"
    }
}
